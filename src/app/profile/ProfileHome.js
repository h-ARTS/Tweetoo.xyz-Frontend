import React from 'react';
import { Typography } from '@material-ui/core';

export default function ProfileHome(props) {
  return (
    <div>
      <Typography>This is Profile Home</Typography>
    </div>
  );
}
